<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.api.dao.MenuInfoDao">  <!--指定mapper对应的dao,这样mapper就会实现dao对应的方法-->

    <select id="queryMenuInfo" resultType="com.example.api.entity.MenuInfo"><!--编写sql语句-->
        SELECT * FROM dining_menu_info
    </select>

</mapper>
